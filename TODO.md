# TODO - Temples Page

- [x] Implement required semantic HTML structure in `temples.html` (head, header/nav, main with 9+ figures, footer).

- [x] Add mobile-first CSS layout and styling in `styles/temples.css` (max-width centered main, nav Flexbox with hover effect, 1-column grid for figures, hamburger styles).

- [x] Add larger view overrides in `styles/temples-large.css` (grid becomes multi-column, hamburger hidden, nav shown).

- [x] Update `scripts/temples.js` to populate footer year/lastModified and implement hamburger toggle.

- [ ] Quick local testing with Live Server; confirm no console errors and responsive behavior.

- [ ] Lighthouse check (mobile + desktop) and confirm images are optimized.

